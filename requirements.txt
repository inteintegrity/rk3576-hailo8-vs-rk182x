# Host-side dependencies: used by the figure scripts and by the screenshot/text checks.
# The board-side runners additionally need numpy and OpenCV (same versions below) plus the
# vendor runtime for their accelerator - see docs/ENVIRONMENT.md.
numpy>=1.24
opencv-python>=4.6
matplotlib>=3.7